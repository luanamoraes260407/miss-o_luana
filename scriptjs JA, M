const caixaPrincipa = document.querySelector (".caixa principal")
const caixaPerguntas = document.querySelector(".caixa-perguntas")
const caixaAlternativas = documet. querySelector(".caixa-alternativas")
const caixaResultado = document.querySelector(".caixa-resultado")
const textoResultado = document.querySelector(".texto-resultado")

const perguntas = [
    {
        enunciado: "qual maneira de se combater o desmatamento?",
        alternativas: [
            "reflorestamento de areas degradadas",
            "implementação de leis mais rigoradas para a proteção de florestas"
        ]
    },
]
 enunciado;"como podemos comater a desigualdade social de forma eficaz e sustentável?",
alternativas;[
"imlementar programas de educação e formação profissional acessíveis a todos",
]


]

let atual = 0;
let PerguntaAtual;

function mostraPergunta(){//criando função para mostra pergunta}
    PerguntaAtual=perguntas[atual];
    caixaPerguntas.textContent = pergunta atual.enunciado;
}





